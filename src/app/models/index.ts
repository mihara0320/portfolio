export interface IProject {
  title: string;
  image: string;
  link: string;
}

export interface ISocial {
  svg: string;
  text: string;
  link: string;
}

export interface IBasics {
  email: string;
  subject: string;
}
